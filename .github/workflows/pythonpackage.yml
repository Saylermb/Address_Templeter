name: Python package

on: [push]

jobs:
  build:

    runs-on: my_server

    steps:
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: test_deploy
      run: |
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
        FOLDER: /
